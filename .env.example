# MediaArr Configuration
# Copy this file to .env and fill in your values

# Required: Get your API key from https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# Optional: Slack webhook for notifications
# Get from https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# Optional: Flask secret key (generate a random string)
SECRET_KEY=change-this-to-a-random-secret-key

# Optional: User and Group IDs for file permissions
# Run `id -u` and `id -g` on your host to get these values
PUID=1000
PGID=1000

# Optional: Custom port (default: 5000)
PORT=5000
